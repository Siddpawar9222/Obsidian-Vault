- Cross Browsing check (How we check if user open application on different platform or browser at same time) 
- Localization (Showing date and time according  to user location or preferred location)
- Retrict app open on different tab (useful on bank application)
- secure cookies
- How  to add filter(name search, DropDown Select, checkbox ) with proper design pattern if we have million of data
- What are  the libraries we used for to show diagram ,  pie charts , validation libraries
- How we can navigate to pages keeping the previous page data if i visited back with  breadCumbs
- How to add plan feature , means If we have different plan, retrict ui functionality according it , Which is correct design pattern for it 
- Debouncing logic for frontend (How to check value already present or not in system). Also check Throttle concept
- Secured way to authetciation(cookies , without storing in localstorage) and authorization in react